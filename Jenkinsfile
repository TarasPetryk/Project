pipeline {
    agent any     
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh "git clone git@github.com:TarasPetryk/clinic.git"
                sh "git branch"
            }
        }        
    }
}
